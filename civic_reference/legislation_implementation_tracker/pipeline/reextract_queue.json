{
  "generated": "2026-08-08",
  "reason": "Laws whose obligations quote reprinted (non-underlined) text, found by pipeline/sweep_restated_duties.py. Re-extract with the marker-aware prompt: the cached text now carries {{...}} around new matter, and EXTRACTION_PROMPT tells the model to extract only from inside those markers when a section is amended to read as follows. Matters already in reextract_exclusions.json are deliberately left out so hand fixes survive. | Aug 11 2026: long laws queued for re-extraction with section windowing (extract_law.split_for_extraction), replacing the old head+tail truncation that dropped their middles. | Aug 11 2026: oversized laws WITHOUT new-matter markers were removed from the queue. Seven of the eight are code recodifications whose bulk is reprinted model code, so extracting them window by window with no way to tell new text from reprinted text would manufacture hundreds of false obligations. They can be requeued once _docx_text emits {{...}} markers from the Word version, which carries the underline runs the PDF discards.",
  "matters": {}
}

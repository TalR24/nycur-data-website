name: Refresh NYC Council Fiscal Impacts

on:
  schedule:
    # Runs on the 1st of each month at 7:00 AM UTC
    - cron: "0 7 1 * *"
  workflow_dispatch:
    inputs:
      years:
        description: "Comma-separated years to search (e.g. 2024,2025,2026)"
        required: false
        default: "2024,2025,2026"
      incremental:
        description: "Skip already-processed matters"
        required: false
        default: "true"
        type: choice
        options: ["true", "false"]

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: pipeline/requirements.txt

      - name: Install Python dependencies
        run: pip install -r pipeline/requirements.txt

      - name: Run fiscal impacts pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          YEARS="${{ github.event.inputs.years || '2024,2025,2026' }}"
          INCREMENTAL="${{ github.event.inputs.incremental || 'true' }}"
          FLAGS="--years $YEARS"
          if [ "$INCREMENTAL" = "true" ]; then FLAGS="$FLAGS --incremental"; fi
          echo "Running: python pipeline/fetch_fiscal_impacts.py $FLAGS"
          python pipeline/fetch_fiscal_impacts.py $FLAGS

      - name: Commit and push updated data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data_website/nyc_council_fiscal_impacts_tracker/data/fiscal_impacts.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            COUNT=$(python3 -c "import json; d=json.load(open('data_website/nyc_council_fiscal_impacts_tracker/data/fiscal_impacts.json')); print(d['metadata']['total_records'])")
            git commit -m "data: refresh fiscal impacts — $COUNT records [$(date -u '+%Y-%m-%d')]"
            git push
          fi
